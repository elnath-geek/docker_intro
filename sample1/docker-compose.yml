version: "3"
services: 
    master: 
        container_name: master
        image: jenkins:latest
        ports:
            - 9000:8080
        volumes:
            - ./jenkins_home:/var/jenkins_home
